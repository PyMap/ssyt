apiVersion: apps/v1
kind: Deployment
metadata:
  name: syt-0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: syt-0
  template:
    metadata:
      labels:
        app: syt-0
    spec:
      containers:
        - name: syt-0
          image: gcr.io/ssyt-342721/ssyt:v1
          resources:
            requests:
              memory: "2000Mi"
